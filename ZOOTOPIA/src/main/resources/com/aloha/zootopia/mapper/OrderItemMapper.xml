<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aloha.zootopia.mapper.OrderItemMapper">
  <resultMap id="OrderItemResult" type="com.aloha.zootopia.domain.OrderItem">
    <id property="id" column="id" />
    <result property="orderId" column="order_id" />
    <result property="orderCode" column="order_code" />
    <result property="productId" column="product_id" />
    <result property="productName" column="product_name" />
    <result property="unitPrice" column="unit_price" />
    <result property="quantity" column="quantity" />
    <result property="lineTotal" column="line_total" />
  </resultMap>

  <sql id="Base_Columns">id, order_id, order_code, product_id, product_name, unit_price, quantity, line_total</sql>

  <insert id="insert" parameterType="com.aloha.zootopia.domain.OrderItem" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO order_items(order_id, order_code, product_id, product_name, unit_price, quantity, line_total)
    VALUES(#{orderId}, #{orderCode}, #{productId}, #{productName}, #{unitPrice}, #{quantity}, #{lineTotal})
  </insert>

  <select id="findByOrderId" parameterType="long" resultMap="OrderItemResult">
    SELECT <include refid="Base_Columns"/> FROM order_items WHERE order_id = #{orderId}
  </select>

  <select id="findByOrderCode" parameterType="string" resultMap="OrderItemResult">
    SELECT <include refid="Base_Columns"/> FROM order_items WHERE order_code = #{orderCode}
  </select>

  <delete id="deleteByOrderId" parameterType="long">
    DELETE FROM order_items WHERE order_id = #{orderId}
  </delete>
</mapper>
