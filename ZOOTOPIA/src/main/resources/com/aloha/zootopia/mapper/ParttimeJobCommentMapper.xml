<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.zootopia.mapper.ParttimeJobCommentMapper">

  <insert id="insertComment" parameterType="com.aloha.zootopia.domain.ParttimeJobComment" useGeneratedKeys="true" keyProperty="commentId">
    INSERT INTO parttime_job_comment (user_id, writer, job_id, content, created_at)
    VALUES (#{userId}, #{writer}, #{jobId}, #{content}, #{createdAt})
  </insert>

  <select id="getCommentById" parameterType="long" resultType="com.aloha.zootopia.domain.ParttimeJobComment">
    SELECT * FROM parttime_job_comment WHERE comment_id = #{commentId}
  </select>

  <delete id="deleteComment" parameterType="long">
    DELETE FROM parttime_job_comment WHERE comment_id = #{commentId}
  </delete>

  <select id="selectCommentsByJobId" parameterType="long" resultType="com.aloha.zootopia.domain.ParttimeJobComment">
    SELECT * FROM parttime_job_comment WHERE job_id = #{jobId} ORDER BY created_at ASC
  </select>

  <select id="countByJobId" parameterType="long" resultType="int">
    SELECT COUNT(*) FROM parttime_job_comment WHERE job_id = #{jobId}
  </select>

  <select id="countAll" resultType="int">
    SELECT COUNT(*) FROM parttime_job_comment
  </select>

  <select id="selectAllPaged" resultType="com.aloha.zootopia.domain.ParttimeJobComment">
    SELECT * FROM parttime_job_comment ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="selectByJobIdPaged" resultType="com.aloha.zootopia.domain.ParttimeJobComment">
    SELECT * FROM parttime_job_comment
    WHERE job_id = #{jobId}
    ORDER BY created_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

</mapper>